import { expFormat } from '../../../helpers/exp-format';
import { module, test } from 'qunit';

module('Unit | Helper | exp format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = expFormat([42]);
  assert.ok(result);
});
