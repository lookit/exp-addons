import { breaklines } from '../../../helpers/breaklines';
import { module, test } from 'qunit';

module('Unit | Helper | breaklines');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = breaklines([42]);
  assert.ok(result);
});
