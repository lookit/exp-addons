<div class="row exp-mood-questionnaire">
  <div class="col-md-12 exp-physics-mood">
    <h4>Mood Questionnaire</h4>
    <p>How are you two doing? We really want to know: we’re interested in how your child’s mood affects which sorts of surprising physical events he/she notices. You can help us find out what babies are really learning as they get older... and what they already knew, but weren’t calm and focused enough to show us!</p>
    {{#bs-form}}
      <label> How is your CHILD feeling right now? </label>
      {{radio-group
        options=moodOptions
        labelLeft="Tired"
        labelRight="Rested"
        value=rested
      }}
      {{radio-group
        options=moodOptions
	labelLeft="Sick"
	labelRight="Healthy"
        value=healthy
      }}
      {{radio-group
        options=moodOptions
	labelLeft="Fussy"
	labelRight="Happy"
        value=childHappy
      }}
      {{radio-group
        options=moodOptions
	labelLeft="Calm"
	labelRight="Active"
        value=active
      }}
      <hr>
      <label> How are YOU feeling right now? </label>
      {{radio-group
        options=moodOptions
	labelLeft="Tired"
	labelRight="Energetic"
        value=energetic
      }}
      {{radio-group
        options=moodOptions
	labelLeft="Overwhelmed"
	labelRight="On top of things"
        value=ontopofstuff
      }}
      {{radio-group
        options=moodOptions
	labelLeft="Upset"
	labelRight="Happy"
        value=parentHappy
      }}
      <hr>
      <div class="form col-md-12">
	<div class="form-group timepicker">
	  <label>
	    About how long ago did your child last wake up from sleep or a nap?
	  </label>
	  {{#if showValidation}}
	  <p class="text text-danger">
	    {{ validations.attrs.napWakeUp.messages }}
	  </p>
	  {{/if}}
	  {{bs-datetimepicker
	  date=napWakeUp
	  format="h:mm"
	  pickDate=false
	  noIcon=true
	  allowInputToggle=true
	  placeholder="hours:minutes"
	  updateDate=(action (mut napWakeUp))}}
	</div>
      </div>
      <br />
      <div class="form col-md-12">
	<div class="form-group">
	  <label>
	    Does your child have a usual nap schedule?
	  </label>
	  {{#if showValidation}}
	  <p class="text text-danger">
	    {{ validations.attrs.usualNapSchedule.messages }}
	  </p>
	  {{/if}}

	  <div class="radio">
	    <label>
	      {{radio-button value="no" checked=usualNapSchedule name="usualNapSchedule"}}
	      No
	    </label>
	  </div>
	  <div class="radio">
	    <label>
	      {{radio-button value="yes" checked=usualNapSchedule name="usualNapSchedule"}}
	      Yes
	    </label>
	  </div>
	  <div class="radio">
	    <label>
	      {{radio-button value="yes-overdue" checked=usualNapSchedule name="usualNapSchedule"}}
	    Yes, and he/she is already due for a nap
	    </label>
	  </div>
	</div>
      </div>
      {{#if (eq usualNapSchedule 'yes') }}
	<div class="form col-md-12">
	  <div class="form-group timepicker">
	  <label>
	    About how much longer until his/her next nap (or bedtime)?
	  </label>
	  {{bs-datetimepicker
	  date=nextNap
	  format="h:mm"
	  pickDate=false
	  noIcon=true
	  allowInputToggle=true
	  placeholder="hours:minutes"
	  updateDate=(action (mut napWakeUp))}}
	</div>
      </div>
      {{/if}}
      <div class="form col-md-12">
	<div class="form-group timepicker">
	  <label>
	    About how long ago did your child last eat or drink?
	  </label>
	  {{#if showValidation}}
	  <p class="text text-danger">
	    {{ validations.attrs.lastEat.messages }}
	  </p>
	  {{/if}}
	  {{bs-datetimepicker
	  date=lastEat
	  format="h:mm"
	  pickDate=false
	  noIcon=true
	  allowInputToggle=true
	  placeholder="hours:minutes"
	  updateDate=(action (mut lastEat))}}
	</div>
      </div>
      <div class="form col-md-12">
	<div class="form-group">
	  <label>
	    What was your child doing before this?
	  </label>
	  {{#if showValidation}}
	  <p class="text text-danger">
	    {{ validations.attrs.doingBefore.messages }}
	  </p>
	  {{/if}}

	  {{input value=doingBefore placeholder="examples: having lunch, playing outside, going to the store with me" class="form-control"}}
	</div>
      </div>
      {{bs-button class="pull-right" defaultText="Continue" type="success" action='continue'}}
    {{/bs-form}}
  </div>
</div>
